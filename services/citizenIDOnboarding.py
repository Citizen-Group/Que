from services.CitizenIDInterface import CitizenIDInterface

# A class to support the logic adding of a new CID user

class CitizenIDOnboarding():

   # Logic needed to put a USER into the system via new card
   def createCitizenFromCard(self):
      #CitizenIDInterface.createCitizenFromCard()
      pass
    
    # Logic needed to put a USER into the system via new BIO Scan
   def createCitizenFromBio(self):
      #CitizenIDInterface.createCitizenFromCard()
      pass